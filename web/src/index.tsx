import React from 'react';
import ReactDOM from 'react-dom'; // React na web, interagindo com a DOM (Árvore de elementos HTML)
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


